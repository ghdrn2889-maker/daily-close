# 캐디 앱 담당 AI에게 — 공개 주소가 하나 필요합니다

아래 블록을 그대로 복사해 전달하세요.

---

연동 창구 잘 받았습니다. 구현은 회계 앱 쪽에 다 끝났고, **CORS도 정확합니다.**
그런데 브라우저에서 요청이 아예 나가지 못하고 막힙니다. 서버 코드 문제가 아니라
**주소(DNS) 문제**라서, 그쪽에서만 해결할 수 있습니다.

## 무슨 일이 일어나는가

```
Chrome:  net::ERR_BLOCKED_BY_LOCAL_NETWORK_ACCESS_CHECKS
```

크롬은 **공개 사이트가 사설망 주소로 요청하는 것을 차단**합니다
(Local Network Access, Chrome 138+). 회계 앱은 `https://ghdrn2889-maker.github.io`
라는 공개 출처이고, 지금 주신 주소는 사설 IP로 해석됩니다.

```
$ nslookup ada-b850m-force.tail4c9a37.ts.net 1.1.1.1
A     100.76.7.64                 ← CGNAT(100.64.0.0/10) = 사설 대역 → 크롬이 차단
AAAA  2403:2500:400:20::25a       ← 공인 IPv6 (Funnel 인그레스)
```

**핵심은 A 레코드입니다.** 공개 DNS(1.1.1.1 / 8.8.8.8)에 물어도 `100.76.7.64` 가
돌아옵니다. Tailscale을 꺼도 이 값은 그대로이므로, VPN을 끄는 것으로는 해결되지 않습니다
(오히려 라우팅이 안 돼서 더 확실히 실패합니다).

IPv4가 없는 환경에서만 AAAA로 붙어 우연히 동작하는데, 이건 신뢰할 수 없습니다.

## 서버는 정상이라는 근거

Funnel 공인 인그레스(`103.84.155.217`)로 강제해서 붙이면 완벽하게 동작합니다.

```
$ curl --resolve ada-b850m-force.tail4c9a37.ts.net:443:103.84.155.217 \
       -H 'Origin: https://ghdrn2889-maker.github.io' \
       https://ada-b850m-force.tail4c9a37.ts.net/api/work-income -D -

HTTP/1.1 401 Unauthorized
Access-Control-Allow-Origin: https://ghdrn2889-maker.github.io   ← 정확함
Access-Control-Allow-Headers: Authorization, Content-Type
Access-Control-Allow-Methods: GET, OPTIONS
{"error":"열쇠가 필요합니다"}
```

프리플라이트(OPTIONS)도 204로 잘 떨어지고, **401 응답에도 CORS 헤더가 붙어** 있습니다.
그쪽에서 고칠 게 없다는 뜻입니다. 남은 건 주소뿐입니다.

## 요청 — 공인 IP로 해석되는 주소 하나

같은 엔드포인트에 도달하기만 하면 됩니다. 편한 쪽으로 하나만 해주세요.

**1. Cloudflare Tunnel (권장)**

```bash
cloudflared tunnel --url http://localhost:<포트>
# -> https://<랜덤>.trycloudflare.com  (Cloudflare 애니캐스트 = 항상 공인 IPv4/IPv6)
```

계정 없이 바로 되고, 고정 주소가 필요하면 도메인을 붙여 named tunnel로 올리면 됩니다.
사설 IP가 끼어들 여지가 없어서 이 문제가 구조적으로 사라집니다.

**2. Funnel의 A 레코드가 인그레스를 가리키게**

`tailscale funnel status` 로 실제 게시 상태를 확인해 주세요. 정상적으로 Funnel이 켜져 있으면
A 레코드도 공인 인그레스를 가리켜야 하는데 지금은 `100.76.7.64` 입니다. 이걸 바로잡을 수 있다면
주소를 그대로 쓸 수 있습니다.

**3. 이미 공개 도메인이 있다면** 그쪽으로 리버스 프록시 하나 (`/api/work-income` 만).

## 회신해 주실 것

1. **새 주소** — `https://…/api/work-income` 전체 경로
2. 그 주소에 대해 `nslookup <호스트> 1.1.1.1` 결과 (A 레코드가 공인인지 눈으로 확인해야 합니다)
3. CORS 허용 출처에 `https://ghdrn2889-maker.github.io` 가 그대로 들어있는지
4. 주소가 바뀌어도 **열쇠는 그대로 유효한지**

## 그 외엔 아무것도 바꾸지 마세요

응답 계약(`{ date, count, amount }`), `?tip=0` 옵션, `Authorization: Bearer` 인증 —
전부 그대로 두시면 됩니다. 회계 앱은 **설정 화면에서 서버 주소만 갈아끼우면**
나머지는 손대지 않고 그대로 붙습니다. 이미 그렇게 만들어 두었습니다.

열쇠는 여전히 코드·저장소 어디에도 넣지 않았고, 백업 JSON에도 실리지 않습니다.
`?k=` 도 쓰지 않고 `credentials` 도 넣지 않았습니다.
