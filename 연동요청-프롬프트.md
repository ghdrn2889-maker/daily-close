# 캐디 앱 담당 AI에게 전달할 연동 요청

아래 블록을 그대로 복사해 전달하세요.

---

안녕하세요. 같은 사용자가 쓰는 **별도의 개인 회계 앱**을 만들고 있습니다.
그 앱이 이 캐디 앱의 **근무·수익 기록을 읽어가야** 해서 협조를 요청드립니다.

## 요청 요약

**읽기 전용 창구 하나만** 만들어 주시면 됩니다. 기존 테이블 구조나 앱 코드는 **바꾸지 마세요.**

```
{ date, count, amount }
```

이 세 필드가 전부입니다. 회계 앱은 이 이상 알 필요가 없고, 알고 싶지도 않습니다.

| 필드 | 타입 | 뜻 |
|---|---|---|
| `date` | `YYYY-MM-DD` 문자열 | 근무한 날짜 |
| `count` | 정수 | 그날의 라운드(부) 수. 개념이 없으면 `1` 고정 또는 생략 |
| `amount` | 정수 | 그날 번 금액 (원 단위, **소수점·콤마 없이**) |

하루에 여러 건이면 **날짜별로 합산**해서 한 줄로 주셔도 되고, 건별로 주셔도 됩니다.
어느 쪽인지만 알려주세요.

## 왜 이렇게 좁게 요청하는가

두 앱을 느슨하게 떼어놓기 위해서입니다.

- 이 세 필드만 유지되면 **캐디 앱 내부는 마음대로 바꾸셔도 됩니다.** 컬럼명을 바꾸든
  테이블을 쪼개든, 창구의 정의만 고치면 회계 앱은 아무것도 모른 채 계속 돕니다.
- 회계 앱은 **절대 쓰기를 하지 않습니다.** 조회만 합니다. 그래서 데이터가 망가질 위험이 없고,
  어느 쪽이 원본인지 다툴 일도 없습니다. 원본은 항상 캐디 앱입니다.
- 팀 구성, 코스, 고객 정보, 팁 내역 같은 건 회계 앱이 알 필요가 없습니다. 넘기지 마세요.

## 구현 방법 (스택에 맞는 것 하나만)

**Supabase / PostgreSQL 이라면** — 뷰 하나면 끝입니다.

```sql
create or replace view public.v_work_income as
select
  <근무일 컬럼>::date            as date,
  coalesce(<라운드수 컬럼>, 1)   as count,
  <금액 컬럼>::bigint            as amount
from public.<근무_테이블>
where <금액 컬럼> > 0;

-- 기반 테이블의 RLS를 그대로 따르게 해서, 본인 데이터만 보이도록
alter view public.v_work_income set (security_invoker = on);
grant select on public.v_work_income to authenticated;
```

**Firebase / Firestore 라면** — 뷰 개념이 없으니 둘 중 하나로:
- 근무 기록을 저장할 때 `work_income/{uid}/days/{YYYY-MM-DD}` 에 위 세 필드를 같이 써두기 (미러링)
- 또는 위 배열을 돌려주는 Cloud Function 엔드포인트 하나

**그 외 / 서버가 있다면** — `GET /api/work-income?from=YYYY-MM-DD&to=YYYY-MM-DD` 로
위 객체의 배열을 돌려주는 읽기 전용 엔드포인트 하나면 됩니다.

## 꼭 지켜주셔야 할 제약

회계 앱은 **서버가 없는 정적 웹앱**입니다. 브라우저에서 직접 조회합니다.

1. **CORS 허용 출처에 이 주소를 추가**해 주세요 — `https://ghdrn2889-maker.github.io`
2. **서비스 키(`service_role`)·관리자 키·비밀번호는 절대 알려주지 마세요.**
   공개 가능한 anon/public 키와 로그인 세션만 씁니다.
3. 데이터 보호는 **RLS(행 수준 보안)로** 해주세요. 본인 계정 데이터만 조회되어야 합니다.
   RLS 없이 전체 공개는 안 됩니다.

## 회신해 주실 내용

작업하신 뒤 아래를 알려주세요. 이게 있어야 회계 앱 쪽을 붙일 수 있습니다.

1. **스택** — Supabase / Firebase / 자체 서버 / 기타
2. **접속 정보** — 프로젝트 URL과 anon(공개) 키
3. **창구 이름** — 뷰 이름 / 컬렉션 경로 / 엔드포인트 URL
4. **인증 방식** — 로그인이 필요한가요? 필요하다면 어떤 방식인지 (이메일+비밀번호, OAuth 등)
5. **실제 응답 예시 1~2건** — 추측이 아니라 **실제로 호출해서 받은 값** 그대로.
   날짜 형식과 금액 타입을 눈으로 확인해야 합니다.
6. **날짜별 합산인지 건별인지**
7. **과거 데이터가 언제부터 있는지** (예: 2026-03-01 부터)

## 참고

회계 앱 쪽은 이미 연결 지점을 비워둔 채 만들어져 있습니다.

```js
async function fetchExternalIncome(fromISO, toISO) { return null; }
```

이 함수 하나만 교체하면 되고, 나머지 코드는 손대지 않습니다.
`null` 을 돌려주는 동안에는 사용자가 직접 기록하는 방식으로 동작합니다.

작업 중 이 계약(세 필드)이 곤란한 부분이 있으면 그대로 말씀해 주세요. 조정하겠습니다.
